# warden-example-simple

Minimal end-to-end usage: sandbox a plain shell command with a broad workspace
allow and one narrower deny, print the result.

## Run it

```
./gradlew :examples:warden-example-simple:run
```
